import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // instrumentationHook is stable in Next 16, config not needed
  },
};

export default nextConfig;
